This docker image bundles the `make_decision.py` script, along with its
dependencies, for use in handling hg-push hooks.

The requirements in `requirements.txt` come from the current latest versions
of

 * jsone
 * requests
 * taskcluster
 * pyyaml
 * slugid
